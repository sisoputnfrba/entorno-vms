name: VM Install Script Check

on:
  push:
    paths:
      - '.github/workflows/base-server.yml'
      - 'base-server.sh'
  pull_request:
    paths:
      - '.github/workflows/base-server.yml'
      - 'base-server.sh'
  workflow_dispatch:

jobs:
  test-install:
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Make script executable
        run: chmod +x ./base-server.sh

      - name: Run installation script
        run: ./base-server.sh

      - name: Verify installed tools
        run: |
          git --version
          gcc --version
          java -version
          ssh -V
